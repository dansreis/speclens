# Tasks: EARS keyword showcase

## 1. Fixture

- [x] Create `examples/ears-markdown-showcase/` repo with `config.json`
- [x] Add a proposal that explains the purpose of the fixture
- [x] Add a spec covering every highlighted keyword at least once
- [x] Include negative cases (code, kbd, lowercase) to confirm they are skipped

## 2. Visual check

- [ ] Open the showcase in light mode — every keyword readable
- [ ] Switch to dark mode — every keyword readable
- [ ] Confirm adjacent keywords on the same line are visually distinguishable
- [ ] Confirm code blocks and `<kbd>` keep default styling
