schema: intent-driven

context: |
  Tech Stack:
    - React 19 + TypeScript + Vite
    - Tauri 2 desktop shell
    - MUI 9 + Emotion

rules:
  proposal:
    - State the "why" before the "what".
  design:
    - Document trade-offs against at least one rejected alternative.
  tasks:
    - Each task must be a verifiable checkbox.
